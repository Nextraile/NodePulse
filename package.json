{
  "name": "nodepulse-mq",
  "version": "1.0.0",
  "description": "Custom Lightweight In-Memory Message Queue Broker built with Vanilla Node.js",
  "main": "./src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types --env-file=.env --watch --inspect=0.0.0.0:9229 ./src/index.ts",
    "check-types": "tsc --noEmit",
    "build": "tsc",
    "test": "node --experimental-strip-types --env-file=.env --test"
  },
  "keywords": [
    "message-queue",
    "broker",
    "vanilla-js"
  ],
  "author": "Nextraile",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  }
}
